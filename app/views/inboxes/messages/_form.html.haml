.message_form
  = form_for Message.new, :url => inboxes.discussion_messages_path(@discussion.id), :method => :post do |f|
    %p
      = f.label :body
      %br
      = f.text_area :body
    %p= f.submit